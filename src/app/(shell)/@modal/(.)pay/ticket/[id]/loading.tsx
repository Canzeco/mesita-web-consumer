import { SheetBodySkeleton } from "@/components/consumer/overlay/DetailSkeletons";

// Renders inside the BottomSheetShell mounted by layout.tsx.
export default function TicketModalLoading() {
  return <SheetBodySkeleton />;
}
