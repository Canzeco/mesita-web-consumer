// Home — discovery hub (Swipe / Social / Favorites). Implementation lands via
// the home-page build agent; this stub only reserves the route.
export const dynamic = "force-dynamic";

export default function HomePage() {
  return null;
}
