// Search — full-screen map + catalog search + Ask AI. Implementation lands via
// the search-page build agent; this stub only reserves the route.
export const dynamic = "force-dynamic";

export default function SearchPage() {
  return null;
}
